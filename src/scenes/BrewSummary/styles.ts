import { StyleSheet } from 'react-native'

const styles = StyleSheet.create({
  buttonContainer: {
    position: 'absolute',
    bottom: 0,
    width: '100%',
  },
  button: {
    marginHorizontal: 12,
  },
})

export default styles
